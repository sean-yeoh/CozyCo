<div id="topics">
  <% @topics.each do |topic| %>
    <div class="topic">
      <h2 class="title">
        <%= link_to topic.title, topic %>
      </h2>
      <p class="date">
        Published at
        <%= time_ago_in_words(topic.created_at) %>
        by
        <%= topic.user.email %>
      </p>
    </div>
  <% end %>
</div>